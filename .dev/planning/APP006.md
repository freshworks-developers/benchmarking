# APP006 — Jira–Freshdesk OAuth sync (planning)

**Use case:** `APP006` · **Type:** Serverless · **Product:** freshdesk (may call Freshservice APIs when mapping targets FS).

## Scope

- **Auth:** Jira OAuth (primary external); Freshworks side uses standard app/install context. `config/oauth_config.json` + `requests.json` for token-backed Jira calls.
- **Install UX:** `iparams.html` / `iparams.js` capture product choice (FD vs FS), Jira project/board keys, and mapping tables (e.g. status, priority, custom fields)—persisted via `iparams.json`.
- **Sync model:** Implement at least one of—**scheduled** (polling Jira / FD APIs) or **webhook** (Jira or Freshdesk events → server handler)—documented in manifest; many builds use both for reliability.
- **Out of scope for criteria file:** Concrete field matrix, rate limits, conflict resolution policy—building agent must still pick a coherent minimal strategy.

## Handoff

Criteria: `test-criteria/APP006-criteria.json`. Next: `python3 bin/setup.py APP006 --criteria-file test-criteria/APP006-criteria.json` then implement per skill / FDK validation.
